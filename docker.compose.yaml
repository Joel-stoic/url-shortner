version: "3.9"

services:
  app:
    build: .
    container_name: backend-app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      BASE_URL: http://localhost:3000
    restart: always